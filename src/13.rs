fn main() {
    // Define variables
    let a = 5;
    let b = 10;

    // Calculate multiplication
    let result = a * b;

    // Print result
    println!("The product of {} and {} is {}", a, b, result);
}
