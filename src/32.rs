fn main() {
    // Example of simple arithmetic operations
    let sum = 2 + 3;
    let difference = 7 - 2;

    println!("The sum is: {}", sum);
    println!("The difference is: {}", difference);

    // Another example with conditional statements
    if true {
        println!("This line will execute if the condition is true");
    } else {
        println!("This line will not execute because the condition is false");
    }
}
